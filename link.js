import OBR from "https://esm.unpkg.com/@owlbear-rodeo/sdk@3.1.0";
import {ROOM_KEY,LEGACY_ROOM_KEY,SYNC_CHANNEL,TOKEN_KEY,LEGACY_TOKEN_KEY,LINK_MODAL_ID,normalizeCatalog,escapeHtml,classLabel} from "./core.js";
let roomId="";
async function init(){await new Promise(r=>OBR.onReady(r));roomId=OBR.room.id;if(await OBR.player.getRole()!=="GM")return OBR.modal.close(LINK_MODAL_ID);const meta=await OBR.room.getMetadata(),cat=normalizeCatalog(meta[ROOM_KEY]||meta[LEGACY_ROOM_KEY],OBR.player.id);document.querySelector("#list").innerHTML=cat.characters.map(c=>`<button class="char" data-id="${escapeHtml(c.id)}"><strong>${escapeHtml(c.name)}</strong><span>${escapeHtml(classLabel(c.class))} // NÍVEL ${c.nivel||1} // NEX ${c.NEX||0}%</span><em>PV ${c.PV?.value??0}/${c.PV?.max??0} · PD ${c.PD?.value??0}/${c.PD?.max??0}</em></button>`).join("")||"<p>Nenhum protagonista criado.</p>";document.querySelector("#loading").remove()}
document.addEventListener("click",async e=>{const b=e.target.closest("[data-id]");if(b){const ids=JSON.parse(localStorage.getItem(`dso.system.pending-link.${roomId}`)||"[]"),meta=await OBR.room.getMetadata(),cat=normalizeCatalog(meta[ROOM_KEY]||meta[LEGACY_ROOM_KEY],OBR.player.id),c=cat.characters.find(x=>x.id===b.dataset.id);if(c&&ids.length){await OBR.scene.items.updateItems(ids,items=>{for(const item of items){item.metadata[TOKEN_KEY]={id:c.id,name:c.name};delete item.metadata[LEGACY_TOKEN_KEY]}});await OBR.broadcast.sendMessage(SYNC_CHANNEL,{type:"token-linked",characterId:c.id},{destination:"ALL"})}await OBR.modal.close(LINK_MODAL_ID)}if(e.target.id==="cancel")await OBR.modal.close(LINK_MODAL_ID)});init();
